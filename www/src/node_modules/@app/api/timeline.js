import * as api from '@app/api'

const urlPrefix = 'timeline';

export function list(token) {
    return api.get(`${urlPrefix}`, {}, token);
}

export function resource(token) {
    return api.get(`${urlPrefix}/resource`, {}, token);
}

export function holiday(token) {
    return api.get(`${urlPrefix}/holiday`, {}, token);
}

export function country(token) {
    return api.get(`${urlPrefix}/country`, {}, token);
}

export function view(id, data, token) {
    return api.get(`${urlPrefix}/${id}`, data, token);
}

export function create(data, token) {
    return api.post(`${urlPrefix}`, data, token);
}

export function update(id, data, token) {
    return api.put(`${urlPrefix}/${id}`, data, token);
}

export function remove(id, token) {
    return api.del(`${urlPrefix}/${id}`, token);
}