import * as api from '@app/api'

const urlPrefix = 'file';

export async function uploadWysiwyg(data, token) {
    return api.post(
        `${urlPrefix}/wysiwyg`, 
        data, 
        token, 
        { 
            'Content-Type': 'multipart/form-data' 
        }
    );
}